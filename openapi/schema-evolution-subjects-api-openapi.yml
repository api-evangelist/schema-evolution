openapi: 3.2.0
info:
  title: Confluent Schema Registry Subjects API
  description: Minimal OpenAPI description of the Confluent Schema Registry REST API used for schema evolution.
  version: 1.0.0
  x-generated-from: https://docs.confluent.io/platform/current/schema-registry/develop/api.html
  x-generated-by: claude-crawl-2026-05-08
servers:
- url: https://your-schema-registry-host
  description: Schema Registry host (replace with your deployment)
tags:
- name: Subjects
paths:
  /subjects:
    get:
      tags:
      - Subjects
      summary: List subjects
      responses:
        '200':
          description: A list of subject names
          content:
            application/vnd.schemaregistry.v1+json:
              schema:
                type: array
                items:
                  type: string
      operationId: getSubjects
      x-operation-id-source: derived
  /subjects/{subject}:
    parameters:
    - $ref: '#/components/parameters/Subject'
    post:
      tags:
      - Subjects
      summary: Look up a schema under a subject
      requestBody:
        $ref: '#/components/requestBodies/Schema'
      responses:
        '200':
          description: Schema metadata
          content:
            application/vnd.schemaregistry.v1+json:
              schema:
                $ref: '#/components/schemas/SchemaMetadata'
      operationId: postSubjectsBySubject
      x-operation-id-source: derived
    delete:
      tags:
      - Subjects
      summary: Delete a subject
      responses:
        '200':
          description: Versions deleted
          content:
            application/vnd.schemaregistry.v1+json:
              schema:
                type: array
                items:
                  type: integer
      operationId: deleteSubjectsBySubject
      x-operation-id-source: derived
  /subjects/{subject}/versions:
    parameters:
    - $ref: '#/components/parameters/Subject'
    get:
      tags:
      - Subjects
      summary: List versions of a subject
      responses:
        '200':
          description: Version list
          content:
            application/vnd.schemaregistry.v1+json:
              schema:
                type: array
                items:
                  type: integer
      operationId: getSubjectsBySubjectVersions
      x-operation-id-source: derived
    post:
      tags:
      - Subjects
      summary: Register a schema under a subject
      requestBody:
        $ref: '#/components/requestBodies/Schema'
      responses:
        '200':
          description: Registered schema id
          content:
            application/vnd.schemaregistry.v1+json:
              schema:
                $ref: '#/components/schemas/SchemaId'
      operationId: postSubjectsBySubjectVersions
      x-operation-id-source: derived
  /subjects/{subject}/versions/{version}:
    parameters:
    - $ref: '#/components/parameters/Subject'
    - $ref: '#/components/parameters/Version'
    get:
      tags:
      - Subjects
      summary: Get a specific version of a schema
      responses:
        '200':
          description: Schema version
          content:
            application/vnd.schemaregistry.v1+json:
              schema:
                $ref: '#/components/schemas/SchemaMetadata'
      operationId: getSubjectsBySubjectVersionsByVersion
      x-operation-id-source: derived
    delete:
      tags:
      - Subjects
      summary: Delete a specific version of a schema
      responses:
        '200':
          description: Deleted version number
          content:
            application/vnd.schemaregistry.v1+json:
              schema:
                type: integer
      operationId: deleteSubjectsBySubjectVersionsByVersion
      x-operation-id-source: derived
components:
  schemas:
    SchemaMetadata:
      type: object
      properties:
        subject:
          type: string
        id:
          type: integer
        version:
          type: integer
        schema:
          type: string
        schemaType:
          type: string
      additionalProperties: true
    SchemaId:
      type: object
      properties:
        id:
          type: integer
      additionalProperties: true
    SchemaString:
      type: object
      description: A schema definition.
      properties:
        schema:
          type: string
        schemaType:
          type: string
        references:
          type: array
          items:
            type: object
            additionalProperties: true
      additionalProperties: true
  requestBodies:
    Schema:
      description: Schema definition
      required: true
      content:
        application/vnd.schemaregistry.v1+json:
          schema:
            $ref: '#/components/schemas/SchemaString'
  parameters:
    Version:
      name: version
      in: path
      required: true
      description: A schema version (numeric or "latest")
      schema:
        type: string
    Subject:
      name: subject
      in: path
      required: true
      description: The subject name
      schema:
        type: string
